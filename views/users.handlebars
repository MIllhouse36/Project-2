<div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h2>These are the games for {{user}}</h2>
        {{#each games}} 
        <p>{{dataValues.title}}</p>
        {{!-- <p>{{summary}}</p>
        <p>{{platform}}</p> --}}
        {{/each}}
      </div>
    </div>
  </div>